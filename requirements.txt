# Note: These are dependencies of famnit-gym package (required by assignment)
numpy>=1.21.0
gymnasium>=1.0.0
pettingzoo>=1.24.0

# Visualization
matplotlib>=3.5.0

# Testing
pytest>=7.0.0

# IMPORTANT: All search algorithms (minimax, alpha-beta pruning) are 
# self-implemented in Python. No external AI or search libraries are used.
# Only the famnit-gym package is used for the game environment, as required.
